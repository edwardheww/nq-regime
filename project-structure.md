nq-regime/
├── main.py              # FastAPI app + scheduler (unchanged)
├── hmm_engine.py        # HMM logic (unchanged)
├── data.py              # Data fetching (unchanged)
├── config.py            # Settings (unchanged)
├── dashboard.py         # Streamlit frontend  ← new
└── requirements.txt